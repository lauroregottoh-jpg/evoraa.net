/**
 * @deprecated Accroche « clin d’œil » retirée de l’UX KELIAA.
 * Conservé uniquement pour éviter les imports orphelins.
 */
export function KeliaaBuddyNudge(_props?: {
  variant?: "card" | "floating"
  href?: string
}) {
  return null
}
